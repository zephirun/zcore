// DbModeToggle removed — DB mode is now managed automatically and silently.
// This file is kept as a stub to avoid import errors from any cached module references.
export function DbModeToggle() { return null; }
